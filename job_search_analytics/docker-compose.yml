version: '3.8'

services:
  postgres:
    image: postgres:15
    container_name: job_search_postgres
    environment:
      POSTGRES_USER: akash
      POSTGRES_PASSWORD: akash123
      POSTGRES_DB: job_search_analytics
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
